import React from "react";

export default function Title({title}) {
    return (
        <div>
            <h1 class="title name text">
                {title}
            </h1>
        </div>
    )
}